name: foodie-react-app
services:
  frontend:
    image: foodie-react-app
    container_name: foodie-react-app
    build: .
    ports:
      - "9060:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
