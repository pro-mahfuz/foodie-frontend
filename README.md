# Foodie Restaurant

A focused restaurant discovery and ordering frontend built from the Foodie Microservices Postman collection.

## Run locally

```bash
npm install
npm run dev
```

The frontend connects to the API gateway at `http://localhost:8080` by default. Change `VITE_API_URL` to use another gateway.

## Build

```bash
npm run build
```
